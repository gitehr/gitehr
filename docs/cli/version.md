# gitehr version

```text
gitehr version
```

Alias: `v`.

Prints the current GitEHR CLI version followed by the underlying Git version, if Git is on `$PATH`.

Example output:

```
GitEHR: 0.1.8
Git 2.51.0
```
